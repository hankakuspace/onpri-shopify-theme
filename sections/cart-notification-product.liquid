{%- if cart != empty -%}
  {%- for item in cart.items -%}
    <div id="cart-notification-product-{{ item.key }}" class="cart-item">
      {%- if item.image -%}
        <div class="cart-notification-product__image global-media-settings">
          <img
            src="{{ item.image | image_url: width: 140 }}"
            alt="{{ item.image.alt | escape }}"
            width="70"
            height="{{ 70 | divided_by: item.image.aspect_ratio | ceil }}"
            loading="lazy"
          >
        </div>
      {%- endif -%}
      <div>
        {%- if settings.show_vendor -%}
          <p class="caption-with-letter-spacing light">{{ item.product.vendor }}</p>
        {%- endif -%}
        <h3 class="cart-notification-product__name h4">{{ item.product.title | escape }}</h3>
        <dl>
          {%- unless item.product.has_only_default_variant -%}
            {%- for option in item.options_with_values -%}
              <div class="product-option">
                <dt>{{ option.name }}:</dt>
                <dd>{{ option.value }}</dd>
              </div>
            {%- endfor -%}
          {%- endunless -%}
          {%- assign onpri_preview_image_url = '' -%}
                          {%- assign has_onpri_customization = false -%}
                          {%- for property in item.properties -%}
                            {%- if property.first == 'ONPRIプレビュー画像URL' and property.last != blank -%}
                              {%- assign onpri_preview_image_url = property.last -%}
                            {%- endif -%}
                            {%- if property.first contains 'ONPRI' and property.last != blank -%}
                              {%- assign has_onpri_customization = true -%}
                            {%- endif -%}
                          {%- endfor -%}

                          {%- if has_onpri_customization -%}
                            <div class="product-option">
                              <span>カスタマイズ済み</span>
                            </div>
                            {%- if onpri_preview_image_url != blank -%}
                              <div class="product-option onpri-cart-preview">
                                <button
                                  type="button"
                                  onclick="document.getElementById('OnpriCartPreviewModal-notification-{{ item.key | handleize }}').style.display = 'flex';"
                                  style="padding: 0; border: 0; background: transparent; cursor: zoom-in;"
                                  aria-label="ONPRIプレビュー画像を拡大表示"
                                >
                                  <img
                                    src="{{ onpri_preview_image_url }}"
                                    alt="{{ item.product.title | escape }} ONPRIプレビュー"
                                    style="width: 72px; height: 72px; object-fit: contain; border: 1px solid #ddd; border-radius: 8px; background: #fff;"
                                  >
                                </button>

                                <div
                                  id="OnpriCartPreviewModal-notification-{{ item.key | handleize }}"
                                  style="display: none; position: fixed; inset: 0; z-index: 2147483647; align-items: center; justify-content: center; padding: 24px; background: rgba(0, 0, 0, 0.6);"
                                >
                                  <div
                                    style="position: relative; z-index: 2147483647; width: min(90vw, 720px); max-height: 90vh; padding: 24px; border-radius: 12px; background: #fff; box-shadow: 0 20px 60px rgba(0, 0, 0, 0.35); overflow: auto;"
                                  >
                                    <button
                                      type="button"
                                      onclick="document.getElementById('OnpriCartPreviewModal-notification-{{ item.key | handleize }}').style.display = 'none';"
                                      style="position: absolute; top: 12px; right: 12px; z-index: 2147483647; border: 0; background: #111; color: #fff; width: 32px; height: 32px; border-radius: 50%; cursor: pointer;"
                                      aria-label="ONPRIプレビュー画像を閉じる"
                                    >
                                      ×
                                    </button>
                                    <img
                                      src="{{ onpri_preview_image_url }}"
                                      alt="{{ item.product.title | escape }} ONPRIプレビュー"
                                      style="display: block; width: 100%; max-height: 80vh; object-fit: contain; background: #fff;"
                                    >
                                  </div>
                                </div>
                              </div>
                            {%- endif -%}
                          {%- endif -%}

                          {%- for property in item.properties -%}
            {%- assign property_first_char = property.first | slice: 0 -%}
                            {%- assign is_onpri_property = false -%}
                            {%- if property.first contains 'ONPRI' or property.first == '_onpri_brand_id' -%}
                              {%- assign is_onpri_property = true -%}
                            {%- endif -%}
            {%- if property.last != blank and property_first_char != '_' and is_onpri_property == false -%}
              <div class="product-option">
                <dt>{{ property.first }}:</dt>
                <dd>
                  {%- if property.last contains '/uploads/' -%}
                    <a href="{{ property.last }}" class="link" target="_blank">
                      {{ property.last | split: '/' | last }}
                    </a>
                  {%- else -%}
                    {{ property.last }}
                  {%- endif -%}
                </dd>
              </div>
            {%- endif -%}
          {%- endfor -%}
        </dl>
        {%- if item.selling_plan_allocation != null -%}
          <p class="product-option">{{ item.selling_plan_allocation.selling_plan.name }}</p>
        {%- endif -%}
      </div>
    </div>
  {%- endfor -%}
{%- endif -%}
